[pytest]
testpaths = agent_app/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=agent_app
    --cov-report=term-missing
    --cov-report=html
asyncio_mode = auto
